"""
Models for the users app.

Since we're using Django's built-in User model, we don't need custom models here.
This file can be used for future custom user-related models if needed.
"""

# No custom models needed - using django.contrib.auth.models.User
